---
name: "git-commit"
description: "分析 git 变更内容并生成标准化的提交信息。当用户需要创建 git 提交或生成提交信息时调用。"
---

# Git 提交助手

## 功能

该技能帮助你分析当前项目的 git 变更状态，并生成标准化的 git 提交信息。

## 工作流程

1. **分析变更**：运行 `git status` 查看变更文件
2. **生成差异**：运行 `git diff` 分析具体变更内容
3. **分类变更**：根据文件类型和变更内容分类（新增、修改、删除）
4. **生成提交信息**：基于变更内容生成符合规范的提交信息
5. **执行提交**：可选执行 `git commit` 命令

## 提交信息规范

生成的提交信息遵循以下格式：

```
<类型>(<范围>): <描述>

<详细信息>

<变更文件列表>
```

**类型**包括：
- feat: 新功能
- fix: 修复 bug
- docs: 文档修改
- style: 代码风格调整
- refactor: 代码重构
- test: 测试相关
- chore: 构建或依赖更新

**范围**：可选，指定变更的模块或目录

**描述**：简短的一句话描述变更内容

**详细信息**：可选，更详细的变更说明

**变更文件列表**：自动生成的变更文件清单

## 示例使用

当用户需要创建 git 提交时，该技能会：
1. 分析当前的 git 变更状态
2. 生成符合规范的提交信息
3. 提示用户确认或修改提交信息
4. 执行 git commit 命令完成提交